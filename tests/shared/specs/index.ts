// tests/shared/specs/index.ts
//
// The one list of pure specs. Both runners import this, so adding a file here makes it
// run headlessly *and* inside Obsidian — there is nowhere else to register it.

import './attachmentFolder.spec';
import './utils.spec';
import './settingsFormat.spec';
